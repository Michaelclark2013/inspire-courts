# Cycle 7 — Phase 6 (Security)

**Date:** 2026-04-25 20:10
**Branch:** cycle/auto-improvements

## Gaps fixed
- /api/gym-events/ical compared the user's `?token=` against the env var with a plain `!==`. Switched to `crypto.timingSafeEqual` so a timing oracle can't extract the token bit-by-bit. The risk is theoretical at HTTP scale but the fix is one helper.

## Outcome
Single commit referencing this report.

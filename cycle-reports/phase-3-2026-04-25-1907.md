# Cycle 5 — Phase 3 (Admin Panel)

**Date:** 2026-04-25 19:07
**Branch:** cycle/auto-improvements
**Phase scope:** Workflow speed, bulk actions, data viz, reporting, role-based access, keyboard shortcuts.

## Current state

- Cmd+K command palette covers ~31 pages.
- Per-row inflight state and dynamic doc titles started landing in cycles 3–4 (5 admin pages had useDocumentTitle).
- ConfirmModal pattern + sticky members modal footer in.

## Gaps found this iteration

### P0 — fix this iteration
1. **Document title coverage** — 70 admin pages still show "Admin Dashboard | Inspire Courts AZ" in the browser tab even when the operator is deep in users / inquiries / billing / owner / maintenance / check-in. Adds tab clutter for anyone juggling 4-5 admin tabs. Hooked `useDocumentTitle` into the highest-traffic five.

### P1 — next iteration
- Bulk actions on Approvals (the API supports `userIds[]` but the UI is still one row at a time).
- Mobile shape for Members / Players / Teams sheets (still horizontal-scroll wide tables on phones).
- Owner Mode could drill from "today's check-ins" → hourly breakdown.
- Equipment "bulk restock" import flow.
- Weekly snapshot email for Mike (cron + email of MRR / churn / red flags).

### P2 — someday
- Per-page hotkey registry (Cmd+S, Cmd+E, Cmd+B, Ctrl+Shift+A).
- Permission gating spot-check across 10 deeper admin pages.
- Admin batch CSV import for players/teams/staff.

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. Add `useDocumentTitle` to: users, inquiries, check-in dashboard, owner mode, billing, maintenance.

## Outcome
Single commit on cycle/auto-improvements referencing this report.

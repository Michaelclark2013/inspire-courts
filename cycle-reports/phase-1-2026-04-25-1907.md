# Cycle 5 — Phase 1 (UI)

**Date:** 2026-04-25 19:07
**Branch:** cycle/auto-improvements
**Phase scope:** Visual design, component polish, accessibility, responsive, loading/error states, brand consistency.

## Current state

- Cycles 3–4 already added focus-visible to most public-facing buttons (SubmitButton, ExportBar CSV button, dashboard cards, EmailSignup, members + plans modals).
- Tap targets and contrast fixes were applied to EmailSignup dark variant and VideoShowcase toggle.
- Members modal got a sticky footer; maintenance kanban switched to 2-col on mobile.
- Most component-level polish lives in `src/components/ui/`. The deeper admin pages still rely on inline class strings instead of shared button primitives.

## Gaps found this iteration

### P0 — fix this iteration
1. **ExportBar print button** — focus-visible was added to the CSV button last cycle but missed on the Print/PDF button. `src/components/ui/ExportBar.tsx`.
2. **EmailSignup loading state** — submit button has `disabled` but no `aria-busy`, so screen readers don't announce the request in flight. `src/components/ui/EmailSignup.tsx`.
3. **LogoUploader close + reset buttons** — the close-X (`p-1`) and remove-image button (`w-7 h-7`) were both under the 44×44 touch target. Already fixed during discovery — bumped to 44×44 with focus rings. `src/components/ui/LogoUploader.tsx`.
4. **PortalAnnouncements dismiss** — `p-1` with `w-3.5 h-3.5` icon = ~18px tap area on a sticky banner. Already fixed during discovery — 44×44 min, slightly larger icon. `src/components/portal/PortalAnnouncements.tsx`.
5. **CheckboxGroup disabled state** — visual weight is identical to active, only opacity drops slightly. Easy to miss the disabled state.

### P1 — next iteration
- Header dropdown items missing min-h-[44px] on desktop.
- Badge neutral variant low contrast on off-white.
- ConfirmModal backdrop missing cursor-pointer.
- KPICard icon color isn't semantic on trend up/down.
- Footer social icons cramped <600px.

### P2 — someday
- SkeletonCard shimmer pacing.
- ClubInterestForm role toggle wrapping <500px.
- VideoShowcase grid gap on small phones.

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. Add focus-visible to ExportBar Print button.
2. Add aria-busy to EmailSignup submit.
3. Tighten CheckboxGroup disabled visual.
4. (Already landed during discovery) LogoUploader + PortalAnnouncements 44×44 fix — keep in this commit batch.

## Outcome
See commits on `cycle/auto-improvements` referencing this report.

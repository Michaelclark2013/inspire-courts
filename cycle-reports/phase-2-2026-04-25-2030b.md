# Cycle 9 — Phase 2 (Functionality)

**Date:** 2026-04-25 20:30 (round B)
**Branch:** cycle/auto-improvements
**Phase scope:** Feature completeness, edge cases, performance, Core Web Vitals, integrations, validation.

## Current state
Cycle 8 phase 2 batched journey-tick member+risk lookups and dropped a
redundant select in `markPastDue`. Cycle 7 phase 2 flagged "Tournament Square
error swallows API error code on failure" as P1 — the registration row had no
visible trace of why a checkout link couldn't be generated, leaving admins
guessing.

Audited webhook signature verification (Square via `WebhooksHelper.verifySignature`,
Twilio via `twilio.validateRequest`) and the v1 API key path (`verifyApiKey` looks
up by SHA-256 hash via SQL `=`, which is fine — the entropy is in the hash, not
the compare). No additional timing-safe-compare gaps surfaced.

## Gaps found this iteration

### P0 — fix this iteration
1. **Tournament Square checkout failure leaves no breadcrumb on the registration row** —
   `src/app/api/tournaments/[id]/register/route.ts:215`. Logged-only failure means an
   admin reviewing pending registrations days later has to dig through Vercel logs to
   figure out what went wrong. Store the truncated error into the existing `notes`
   column so it surfaces in the admin UI alongside the row.

### P1 — next iteration
- `runBillingTick` calls `chargeSubscription` sequentially. For dunning days with
  many retries this serializes Square API calls; could be bounded-parallel.
- `chargeSubscription` does separate updates to `invoices`, `subscriptions`, and
  `members` — could be one transaction for atomicity.
- Tournament division capacity ignores pending-but-not-paid registrations on the
  duplicate-block check.

### P2 — someday
- Webhook delivery retry job (right now `failureCount` increments on each delivery
  failure but nothing actively retries them).

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. Stamp Square checkout failure into `tournamentRegistrations.notes` (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.

# Cycle 5 — Phase 5 (Customer Acquisition & Marketing)

**Date:** 2026-04-25 19:07
**Branch:** cycle/auto-improvements
**Phase scope:** SEO audit, content strategy, email capture, analytics, local SEO.

## Current state

- LocalBusiness, FAQPage, ItemList, SportsActivityLocation, SportsEvent JSON-LD already shipped.
- Cycle 3 added Twitter cards on the four sport pages.
- Mobile sticky register CTA + Meta Pixel + GA4 are wired.
- Cycle 5 Phase 4 added BreadcrumbList to /facility and /gameday.

## Gaps found this iteration

### P0 — fix this iteration
1. **BreadcrumbList JSON-LD missing on 4 more public landing pages** — /about, /teams, /training, /camps. Without breadcrumbs, Google won't render the breadcrumb rich snippet on SERPs and the page-hierarchy signal is weaker. Added.
2. **GA4 standard events missing for key conversions** — `trackConversion` only emits Meta standard events for 4 of the 9 conversion types and never emits a GA4 `generate_lead`, `begin_checkout`, `sign_up`, or `select_promotion`. So you can't build funnels in GA4 (which Mike runs alongside Meta). Mapped each of the 9 cases to its GA4 equivalent.

### P1 — next iteration
- AggregateRating schema on homepage / facility (would unlock the ⭐⭐⭐⭐⭐ rich snippet — needs Mike to confirm a current count + average).
- EmailSignup variant in the SportMicrosite component so basketball/volleyball/pickleball/futsal landing pages capture leads at lower friction than the inquire form.
- Internal linking from sport pages → facility / about / contact for local-SEO authority.
- Offer schema on /book + per-sport rental sections.

### P2 — someday
- Consolidate duplicate LocalBusiness across facility/about/sports under @id fragments.
- Email consent checkbox on EmailSignup (CASL/GDPR if Mike emails Canadians/EU).
- Per-sport Meta Pixel `view_sport_page` for audience segmentation.
- Pickleball page title rewrite to lean on "indoor pickleball Gilbert / Phoenix summer heat" intent.

### BLOCKED — needs Mike
- AggregateRating schema needs an actual review count + average from Google Business / Yelp to be honest.
- `NEXT_PUBLIC_GOOGLE_REVIEW_URL` still placeholder.

## Tasks chosen for ACT step
1. BreadcrumbList JSON-LD on /about, /teams, /training, /camps (DONE).
2. GA4 standard event mapping in `trackConversion` for all 9 conversion types (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.

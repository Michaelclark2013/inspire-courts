# Cycle 8 — Phase 4 (Public Site)

**Date:** 2026-04-25 20:30
**Branch:** cycle/auto-improvements
**Phase scope:** Conversion optimization, page speed, SEO, schema markup, CTAs, mobile experience.

## Current state
BreadcrumbList JSON-LD has been baked into most public top-nav pages over the last few
cycles. Audit shows /events, /faq, /contact, /open-gym still missing it — these pages
already have other LD blocks (FAQPage, LocalBusiness, SportsActivity) and are high-value
SEO targets.

## Gaps found this iteration

### P0 — fix this iteration
1. **BreadcrumbList LD on /events** — primary tournament discovery surface.
2. **BreadcrumbList LD on /faq** — alongside the existing FAQPage schema.
3. **BreadcrumbList LD on /contact** — alongside existing LocalBusiness schema.
4. **BreadcrumbList LD on /open-gym** — alongside existing SportsActivity schema.

### P1 — next iteration
- Several auth/utility pages (/login, /register, /forgot-password, /reset-password,
  /verify-email) lack any LD; these aren't SEO targets but adding canonical URLs
  would clean up duplicate-content signals.
- /book and /prep both lack breadcrumbs.

### P2 — someday
- Content audit on auto-generated `/sport/[slug]` microsites for unique meta descriptions.

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. /events breadcrumb LD (DONE).
2. /faq breadcrumb LD (DONE).
3. /contact breadcrumb LD (DONE).
4. /open-gym breadcrumb LD (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.

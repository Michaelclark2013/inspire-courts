# Cycle 9 — Phase 1 (UI)

**Date:** 2026-04-25 20:30 (round B)
**Branch:** cycle/auto-improvements
**Phase scope:** Visual design, component polish, accessibility, responsive, loading/error states, brand consistency.

## Current state
Past few cycles converted the "Loading…" string on plans, time-off, programs,
inbox, and program-detail to `SkeletonRows`. Audit shows three more admin pages
still ship the bare-text loading state: workouts, integrations, profile.

## Gaps found this iteration

### P0 — fix this iteration
1. **/admin/workouts** — replace `<div>Loading…</div>` with SkeletonRows.
2. **/admin/integrations** — replace `<div>Loading…</div>` with SkeletonRows.
3. **/admin/profile** — replace `<div>Loading…</div>` with SkeletonRows.

### P1 — next iteration
- /admin/availability uses inline "Loading…" text inside a header rather than
  a full-page spinner; could be tightened with a small skeleton.
- /admin/payroll, /admin/certifications, /admin/maintenance still use bare
  loading strings (single-line, harder to tell at-a-glance).

### P2 — someday
- Page-level error boundaries on /admin/* surfaces (some have `error.tsx`,
  many don't).

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. SkeletonRows on /admin/workouts (DONE).
2. SkeletonRows on /admin/integrations (DONE).
3. SkeletonRows on /admin/profile (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.

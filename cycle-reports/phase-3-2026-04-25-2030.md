# Cycle 8 — Phase 3 (Admin Panel)

**Date:** 2026-04-25 20:30
**Branch:** cycle/auto-improvements
**Phase scope:** Workflow speed, bulk actions, data viz, reporting, role-based access, keyboard shortcuts.

## Current state
Tab titles previously rolled out across most admin pages; three more high-traffic admin
pages still inherit the layout title in browser tabs: Programs, Membership plans,
Broadcasts. All three are client components — the established pattern is
`useDocumentTitle("…")`.

## Gaps found this iteration

### P0 — fix this iteration
1. **Tab title on /admin/programs** — `useDocumentTitle("Programs")`.
2. **Tab title on /admin/membership-plans** — `useDocumentTitle("Membership plans")`.
3. **Tab title on /admin/broadcasts** — `useDocumentTitle("Broadcasts")`.

### P1 — next iteration
- Bulk-action UI on Approvals.
- Mobile card layout for the Players / Teams / Staff sheets.
- Per-page hotkey registry.

### P2 — someday
- Weekly snapshot email.
- Permission gating spot-check across deeper admin pages.

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. Programs (DONE).
2. Membership plans (DONE).
3. Broadcasts (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.

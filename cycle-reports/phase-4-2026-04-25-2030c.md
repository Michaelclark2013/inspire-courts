# Cycle 10 — Phase 4 (Public Site)

**Date:** 2026-04-25 20:30 (round C)
**Branch:** cycle/auto-improvements

## Gap fixed
/tournaments/[id] (the dynamic per-tournament detail page) shipped a
`SportsEvent` JSON-LD block but no `BreadcrumbList` — Google's tournament
listing card had no trail to render. Added a 3-level breadcrumb
(Home › Tournaments › <tournament name>) using the cached tournament
fetch so it doesn't trigger a second DB hit.

Also tightened the SportsEvent `location.address` block — added
`streetAddress` and `postalCode` so Google's structured-data validator
no longer flags the Place as missing required fields.

## Outcome
Single commit referencing this report.

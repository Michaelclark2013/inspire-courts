# Cycle 6 — Phase 4 (Public Site)

**Date:** 2026-04-25 19:35
**Branch:** cycle/auto-improvements
**Phase scope:** Conversion optimization, page speed, SEO, schema markup, CTAs, mobile experience.

## Current state

Cycle 5 already added BreadcrumbList JSON-LD to /facility, /gameday, /about, /teams, /training, /camps. Two more high-traffic public pages still lacked it.

## Gaps found this iteration

### P0 — fix this iteration
1. **/scores missing BreadcrumbList** — added.
2. **/schedule missing BreadcrumbList** — added.

### P1 — next iteration
- /gallery and /media still missing BreadcrumbList.
- /tournaments has ItemList but no AggregateOffer rollup.
- Hero CTA padding < 375px wraps on the homepage.

### P2 — someday
- Compress + responsive srcSet on courts-bg.jpg (518KB).
- Hero `<Image priority={false}>` on pages where the hero isn't the LCP element.

### BLOCKED — needs Mike
- `NEXT_PUBLIC_GOOGLE_REVIEW_URL` placeholder.

## Tasks chosen for ACT step
1. BreadcrumbList JSON-LD on /scores (DONE).
2. BreadcrumbList JSON-LD on /schedule (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.

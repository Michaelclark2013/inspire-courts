# Cycle 9 — Phase 3 (Admin Panel)

**Date:** 2026-04-25 20:30 (round B)
**Branch:** cycle/auto-improvements
**Phase scope:** Workflow speed, bulk actions, data viz, reporting, role-based access, keyboard shortcuts.

## Current state
The tab-title rollout has covered most admin surfaces over the last several cycles.
Three more pages still inherit the layout title: Rentals, Security, Permissions —
all client components, so the standard `useDocumentTitle` pattern applies.

## Gaps found this iteration

### P0 — fix this iteration
1. **/admin/rentals tab title** — high-traffic court-rental review page.
2. **/admin/security tab title** — security ops page (force re-login, audit feed).
3. **/admin/permissions tab title** — per-user permission overrides page.

### P1 — next iteration
- Bulk-action UI on Approvals.
- Mobile card layout for Players / Teams / Staff sheets.
- Per-page hotkey registry.

### P2 — someday
- Weekly snapshot email.
- Permission gating spot-check across deeper admin pages.

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. /admin/rentals (DONE).
2. /admin/security (DONE).
3. /admin/permissions (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.

# Cycle 12 — Phase 5 (Marketing)

**Date:** 2026-04-25 20:30 (round E)
**Branch:** cycle/auto-improvements

## Gap fixed
Homepage `SportsActivityLocation` LD was missing two recommended fields:

- **`image`** — Google flags LocalBusiness types as ineligible for rich
  results without an image. Added `${SITE_URL}/opengraph-image` so the
  same dynamic 1200×630 used for social shares becomes the structured-data
  image too.
- **`priceRange`** ("$$") — drives the price-tier indicator in the
  knowledge panel; matches what we just added to /contact's
  LocalBusiness.

## Outcome
Single commit referencing this report.

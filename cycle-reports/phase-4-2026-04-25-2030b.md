# Cycle 9 — Phase 4 (Public Site)

**Date:** 2026-04-25 20:30 (round B)
**Branch:** cycle/auto-improvements
**Phase scope:** Conversion optimization, page speed, SEO, schema markup, CTAs, mobile experience.

## Current state
Last cycle added BreadcrumbList LD to /events, /faq, /contact, /open-gym, /book,
/prep. Audit shows /privacy and /terms still missing breadcrumb LD — both are
indexed (`robots: { index: true }`) and routinely show up as "informational"
results when people search "Inspire Courts privacy" or "terms". Adding
breadcrumbs lets Google render the home › privacy trail in SERP.

/waiver is `robots: { index: false }` so it doesn't benefit from breadcrumb LD.

## Gaps found this iteration

### P0 — fix this iteration
1. **BreadcrumbList LD on /privacy** — indexed legal page, no breadcrumb LD.
2. **BreadcrumbList LD on /terms** — indexed legal page, no breadcrumb LD.

### P1 — next iteration
- Auth utility pages (/login, /register, /forgot-password, /reset-password,
  /verify-email) lack canonical URLs. They're robots-disallowed but Google
  occasionally indexes disallowed pages from external links anyway.
- /tournaments/[id]/register/confirmation page has no canonical URL.
- /scores/live/[gameId] has no canonical URL.

### P2 — someday
- Sport microsites all share `/images/courts-bg.jpg` for OG; per-sport OGs would
  lift CTR from social shares.

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. /privacy breadcrumb LD (DONE).
2. /terms breadcrumb LD (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.

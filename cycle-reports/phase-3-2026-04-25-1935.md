# Cycle 6 — Phase 3 (Admin Panel)

**Date:** 2026-04-25 19:35
**Branch:** cycle/auto-improvements
**Phase scope:** Workflow speed, bulk actions, data viz, reporting, role-based access, keyboard shortcuts.

## Current state

- Cycle 5 covered owner / billing / maintenance / users / inquiries / check-in with `useDocumentTitle`.
- Three more lists got SkeletonRows in Phase 1 of this cycle.

## Gaps found this iteration

### P0 — fix this iteration
1. **Tab title coverage on inbox + audit log + sponsors + contacts** — these high-traffic admin surfaces still inherited the layout's static title. Wired up `useDocumentTitle` (client) or page-level `metadata.title` (server) on each.

### P1 — next iteration
- Remaining 60+ admin client pages still use the layout title.
- Bulk-action UI on Approvals (API supports `userIds[]` already).
- Mobile card view for Players / Teams / Staff sheets (still horizontal-scroll wide tables).

### P2 — someday
- Per-page hotkey registry (Cmd+S, Cmd+E, Cmd+B).
- Weekly snapshot email cron for Mike.

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. `useDocumentTitle("Inbox")` on `/admin/inbox`.
2. `useDocumentTitle("Audit Log")` on `/admin/audit-log`.
3. `metadata.title` on `/admin/sponsors` (server component).
4. `metadata.title` on `/admin/contacts` (server component).

## Outcome
Single commit on cycle/auto-improvements referencing this report.

# Cycle 1 · Phase 5 (Customer Acquisition & Marketing) · 2026-04-25 10:23

## Scope
SEO, content, email capture, retention, referral, social proof, analytics events, A/B infra, retargeting, local SEO, review generation.

## Discovery — gaps in current marketing surface

### P0 — attribution + funnel tracking
1. **`inquire_form_submit` event isn't fired anywhere.** The event is defined in analytics types but the InquiryForm doesn't call it on success. Funnel tracking is broken.
2. **UTM params aren't auto-captured** in inquiry submissions. Visitors arriving via `?utm_source=instagram&utm_campaign=summer-camps` lose attribution because the form only reads `source` from a default page-slug.
3. **No short-link / redirect system.** SMS campaigns and QR codes rely on shortened, trackable URLs. Right now any short URL has to live external (bit.ly, goo.gl) which leaks data + costs reliability.

### P0 — review generation
4. **No `/reviews` redirect page** to funnel happy customers to Google reviews. Front desk can text a link after a great session — without a clean redirect URL, it's a 70-character Google review URL that gets blocked / mistyped.

### P1 — capture
5. **EmailSignup is not on sport microsites.** Visitors not ready to inquire have no way to opt in for email updates.
6. **No exit-intent capture** on inquiry pages.

### P2
7. No A/B test infra (flag-driven variant on hero copy).
8. No "as seen in" / press logo strip anywhere.

## Chosen tasks

1. **Fire `inquire_form_submit`** in InquiryForm on success with metadata `{kind, source}`.
2. **Auto-capture UTM** params from URL on inquiry pages — fold `utm_source/utm_medium/utm_campaign` into `source`.
3. **Short-link / redirect system** — new table `short_links` + `/r/[slug]` route that 302s and logs the click. Admin UI deferred to next pass.
4. **`/leave-a-review` redirect page** — clean URL → Google review redirect. Simple text version for SMS share.

## What was done

### Shipped
1. **`inquire_form_submit` conversion event** now fires from InquiryForm on success → goes to Google Analytics + Meta Pixel + any future tools.
2. **`inquire_submitted` detailed event** with `{kind, source, utm_source, utm_campaign}` so funnels can be sliced in GA4.
3. **UTM auto-capture** in InquiryForm — reads `utm_source / utm_medium / utm_campaign` from URL on mount, folds them into the `source` field as `inquire/<slug>|utm:<source>/<campaign>`. Also stored in `details_json` for full attribution detail.
4. **Short-link redirect system** — new `short_links` table + `/r/[slug]` route. 302 redirect with click counter + active/expires guards. Migration 0038.
5. **`/leave-a-review`** smart-routing page — 5-star CTA goes to Google review URL (env-controlled `NEXT_PUBLIC_GOOGLE_REVIEW_URL`); "something was off" CTA routes to inquiry form so detractors are intercepted privately. Classic NPS amplify-promoters / intercept-detractors split. Robots-noindex'd (campaign destination, not for search).

### Files changed / added
- `src/components/inquiry/InquiryForm.tsx` — UTM capture, analytics events
- `src/lib/db/schema.ts` — `shortLinks` table
- `drizzle/0038_short_links.sql`
- `src/app/r/[slug]/route.ts` (new redirect handler)
- `src/app/leave-a-review/page.tsx` (new)

### Build status
✅ `npm run build` — 9.3s, 0 errors. New routes registered: `/r/[slug]`, `/leave-a-review`.

### Decisions made autonomously
- Short-link admin UI deferred — admins can insert rows directly into `short_links` for now. UI is a phase 3 follow-up.
- Set `NEXT_PUBLIC_GOOGLE_REVIEW_URL` to a placeholder. Mike must replace with the real Google review link when posting `/leave-a-review` URL anywhere. Falls back to placeholder URL until then.
- Short-link slug typos / unknowns 302 to home rather than 404. Less friction in the wild.

### BLOCKED — needs Mike
- **`NEXT_PUBLIC_GOOGLE_REVIEW_URL` env var** needs to be set in Vercel with the real Google review URL. Currently uses a placeholder. Without this, the 5-star CTA leads to a broken link. Listed here so we can pick it up post-rotation.

### Cross-cutting (Claude Code project hygiene)
- Could add `.claude/commands/mint-short-link` slash command that prompts for slug + destination + label and inserts a row. Logged for phase 7.

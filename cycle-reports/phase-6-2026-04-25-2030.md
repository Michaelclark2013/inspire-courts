# Cycle 8 — Phase 6 (Security)

**Date:** 2026-04-25 20:30
**Branch:** cycle/auto-improvements
**Phase scope:** OWASP top 10, secrets, dependencies, CSP, auth hardening, rate limiting, PII.

## Current state
Cycle 7 phase 6 fixed a timing-attack on `/api/gym-events/ical` by switching from `!==`
to `crypto.timingSafeEqual`. Audit shows the same class of bug in
`requireCronSecret` (`src/lib/api-helpers.ts:90`) — it uses plain `!==` to compare
`CRON_SECRET` against the request-supplied value. Because this single helper
gates every cron endpoint (billing-tick, journey-tick, churn-tick, member-daily,
digest, maintenance, inquiry-sla-tick, clean-expired-permissions), one fix covers
the whole cron surface.

## Gaps found this iteration

### P0 — fix this iteration
1. **Cron secret compared with `!==`** — `src/lib/api-helpers.ts:90`. Same
   timing-oracle class fixed last cycle for the iCal token. Replaced with
   `crypto.timingSafeEqual` via a local constant-time helper. Existing
   api-helpers test suite (25 tests) continues to pass.

### P1 — next iteration
- `verifyApiKey` (in `src/lib/api-auth.ts`) likely has the same plain compare on
  the bearer-token hash; worth auditing.
- Twilio + Square webhook signature verification — confirm both use timing-safe
  HMAC compare via the SDKs (looked OK at a glance but warrants a closer pass).

### P2 — someday
- CSP audit — verify `script-src` doesn't allow `'unsafe-eval'` in production
  (we ship Recharts which historically needed it).

### BLOCKED — needs Mike
None this phase. Blocked-list carry-over: `NEXT_PUBLIC_GOOGLE_REVIEW_URL` and
Square live keys still pending on Vercel.

## Tasks chosen for ACT step
1. Replace `!==` with `crypto.timingSafeEqual` in `requireCronSecret` (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.
After this commit, push branch and open PR titled `Cycle 8`.

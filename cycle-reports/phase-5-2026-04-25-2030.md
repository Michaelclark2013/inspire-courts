# Cycle 8 — Phase 5 (Marketing)

**Date:** 2026-04-25 20:30
**Branch:** cycle/auto-improvements
**Phase scope:** SEO audit, content strategy, email capture, analytics, local SEO.

## Current state
Audited the SEO infrastructure (`src/app/robots.ts`, `src/app/sitemap.ts`,
breadcrumb LD coverage). Two findings:

1. `/leave-a-review` is listed in the sitemap (priority 0.5) but explicitly
   disallowed in robots.ts. Crawlers see this as a contradiction; the
   campaign page is meant to be a redirect surface, not an indexable page.
2. `/book` and `/prep` ship rich landing-page metadata but no BreadcrumbList LD —
   both are high-conversion surfaces (facility rentals, prep academy).

## Gaps found this iteration

### P0 — fix this iteration
1. **Remove /leave-a-review from sitemap** — it's robots-disallowed; sitemap entry was misleading.
2. **BreadcrumbList LD on /book** — facility rental page; lacked breadcrumb LD.
3. **BreadcrumbList LD on /prep** — Inspire Prep landing page; ships EducationalOrganization LD but no breadcrumb.

### P1 — next iteration
- Auth utility pages (`/login`, `/register`, `/forgot-password`, `/reset-password`,
  `/verify-email`) lack canonical URLs. They're robots-disallowed, but Google has
  been known to index disallowed pages anyway when external links exist.
- Sport microsites use the same generic OG image — could swap in sport-specific OGs
  to lift CTR from social shares.

### P2 — someday
- GA4 conversion event audit on inquire / register / book flows.
- Local SEO: ensure NAP (name/address/phone) is consistent across LocalBusiness
  blocks on /contact and SportsActivityLocation on sport microsites.

### BLOCKED — needs Mike
- `NEXT_PUBLIC_GOOGLE_REVIEW_URL` env var still placeholder (carry-over).

## Tasks chosen for ACT step
1. Drop /leave-a-review sitemap entry (DONE).
2. /book breadcrumb LD (DONE).
3. /prep breadcrumb LD (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.

# Cycle 7 — Phase 1 (UI)

**Date:** 2026-04-25 20:10
**Branch:** cycle/auto-improvements
**Phase scope:** Visual design, component polish, accessibility, responsive, loading/error states, brand consistency.

## Current state
Many admin lists still use a plain "Loading…" string instead of `<SkeletonRows>`. Cycle 6 Phase 1 swapped three; this phase swaps three more.

## Gaps found this iteration

### P0 — fix this iteration
1. **Plain "Loading…" text on /admin/membership-plans, /admin/time-off, /admin/programs** — Each replaced with `<SkeletonRows count={4} />`.

### P1 — next iteration
- Six more pages still using plain "Loading…" text (rentals/[id], workouts, gym-schedule, resources/[id], permissions/[userId] x 2, integrations).
- Badge `neutral` variant low-contrast on off-white card backgrounds.

### P2 — someday
- ExportBar buttons under 44×44.
- SkeletonCard shimmer pacing tuning.

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. SkeletonRows on /admin/membership-plans (DONE).
2. SkeletonRows on /admin/time-off (DONE).
3. SkeletonRows on /admin/programs (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.

# Cycle 2 · Phase 1 (UI) · 2026-04-25 11:00 — interrupt-driven

User reported three concrete issues mid-rotation. Pivoted from fresh
phase-1 discovery to the reported pain since (a) it's higher leverage
than speculative UI polish and (b) two of the three were security
correctness bugs.

## Shipped
1. **Scorekeeper-name guard** — admin + portal score-write APIs reject
   when session.user.name is empty. UI banner on /admin/scores/enter
   shows "Logged in as <Name>".
2. **Add Staff button** on /admin/roster, deep-linked to
   /admin/users?role=staff.
3. **Permissions expiresAt drop** at 3 layers: API GET handler,
   session callback, and 2 UI consumers (sidebar + button grid). Was
   showing expired overrides as live — confusing because middleware
   correctly ignored them. Fixed everywhere.
4. **Profile page crash** when me.name was nullish — guarded initials
   compute + heading fallback.

## BLOCKED — needs Mike
- "issues when playing with admin permissions" — addressed the most
  likely root cause (expiresAt drop). If symptoms persist, need
  reproduction steps.
- "profile page doesn't work" — addressed the crash-on-null-name path.
  If something else is broken, need details (which page, which action,
  what error).

Build: 0 errors.
Commit: 8412698 on main.

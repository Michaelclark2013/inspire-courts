# Cycle 6 — Phase 2 (Functionality)

**Date:** 2026-04-25 19:35
**Branch:** cycle/auto-improvements
**Phase scope:** Feature completeness, edge cases, performance, Core Web Vitals, integrations, validation.

## Current state

- Public-form error handling, IDOR fixes, and zod validation cover most surfaces.
- Recent perf wins include cached tournament fetch, narrowed gameScore selects, and the portal-summary LEFT JOIN.

## Gaps found this iteration

### P0 — fix this iteration
1. **Check-in nudge sent emails sequentially** — `src/app/api/admin/checkin-progress/nudge/route.ts:105`. For a 30-team tournament that's 30+ seconds of wall time. Replaced with `Promise.allSettled` so all sends fan out at once and one bad address can't short-circuit the batch.
2. **Floor-status N+1 lookup** — `src/app/api/admin/floor-status/route.ts:71`. Each of 7 courts ran three `.find()` scans across liveGames, upcomingGames, and todayEvents = 21 scans. Indexed each source array into a `Map` once and looked up in O(1). Doesn't matter for 7 courts but the pattern matters when the floor grows.

### P1 — next iteration
- `src/lib/journeys.ts:158` runs an N+1 step lookup per active enrollment.
- `src/lib/billing.ts` does single-row `.select().from(subscriptions)` in loops.
- Tournament registration's Square error logs the *intent* but not the Square error code; admin can't triage.
- Notion-disabled fallback returns `[]` silently — surfaces "no events" instead of "Notion not configured".

### P2 — someday
- Email recipient regex is permissive (`includes("@")`) — could add stricter validation before send.
- Tournament division capacity ignores pending-but-not-paid registrations on the duplicate-block check.

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. Parallelize nudge emails (DONE).
2. Index floor-status lookups into Maps (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.

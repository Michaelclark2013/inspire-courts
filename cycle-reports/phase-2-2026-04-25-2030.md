# Cycle 8 — Phase 2 (Functionality)

**Date:** 2026-04-25 20:30
**Branch:** cycle/auto-improvements
**Phase scope:** Feature completeness, edge cases, performance, Core Web Vitals, integrations, validation.

## Current state
Cycle 7 phase 2 narrowed `runJourneyTick`'s "find next step" lookup from full table scan
to indexed `WHERE ordering > current LIMIT 1`. Two sibling N+1 patterns remain — member
fetch and risk-score fetch are still per-enrollment. `markPastDue` re-selects the
subscription row to recover memberId the caller already has.

## Gaps found this iteration

### P0 — fix this iteration
1. **Journey tick: per-enrollment member + risk score lookups** — `src/lib/journeys.ts:112-126`. For 200 due enrollments that's 400 round-trips of single-row reads. Pre-fetch all members + all risk scores in two batched `WHERE memberId IN (...)` queries, build a Map, look up by id inside the loop.
2. **Billing tick: `markPastDue` re-selects the subscription it was just called from** — `src/lib/billing.ts:301`. The caller (`chargeSubscription`) already has `sub` in scope; passing `memberId` as a parameter removes one DB round-trip per failure path.

### P1 — next iteration
- Tournament Square checkout link error swallows the structured error code into a generic message. Storing the code on `tournamentRegistrations.notes` would let admins see why a link failed.
- `runBillingTick` calls `chargeSubscription` sequentially. For dunning days with many retries this serializes Square API calls; could be bounded-parallel (e.g. 5 at a time).
- `chargeSubscription` does separate updates to `invoices`, `subscriptions`, and `members` — could be one transaction for atomicity.

### P2 — someday
- Tournament division capacity ignores pending-but-not-paid registrations on the duplicate-block check.
- Step lookup in journey tick could also be pre-batched (smaller win since `(journeyId, ordering)` is indexed).

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. Batch member + risk score fetches in `runJourneyTick`.
2. Pass `memberId` into `markPastDue` to drop the redundant subscription select.

## Outcome
Two commits on cycle/auto-improvements referencing this report.

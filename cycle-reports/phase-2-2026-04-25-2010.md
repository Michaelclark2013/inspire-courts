# Cycle 7 — Phase 2 (Functionality)

**Date:** 2026-04-25 20:10
**Branch:** cycle/auto-improvements
**Phase scope:** Feature completeness, edge cases, performance, Core Web Vitals, integrations, validation.

## Current state
Recent perf wins: parallel nudge emails, indexed floor-status, narrowed gameScore selects, portal/summary LEFT JOIN, tournament cache().

## Gaps found this iteration

### P0 — fix this iteration
1. **Journey tick re-fetched all steps on every advance** — `src/lib/journeys.ts:158`. After firing each enrollment's current step, the code pulled every step in the journey just to find the next one. For a 10-step journey with 200 active enrollments that's 200 full-table scans. Replaced with `WHERE ordering > current ORDER BY ordering LIMIT 1` — the index does the work.

### P1 — next iteration
- Journey tick still pulls member + memberRiskScores per enrollment one-at-a-time; could be batched.
- `src/lib/billing.ts` does single-row subscription lookups in loops.
- Tournament Square error swallows API error code on failure.

### P2 — someday
- Tournament division capacity ignores pending-but-not-paid registrations on the duplicate-block check.

### BLOCKED — needs Mike
None this phase.

## Tasks chosen for ACT step
1. Replace full-step-list fetch in journey tick with a single bounded query (DONE).

## Outcome
One commit on cycle/auto-improvements referencing this report.

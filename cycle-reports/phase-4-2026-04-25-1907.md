# Cycle 5 — Phase 4 (Public Site)

**Date:** 2026-04-25 19:07
**Branch:** cycle/auto-improvements
**Phase scope:** Conversion optimization, page speed, SEO, schema markup, CTAs, mobile experience.

## Current state

- Most public pages have JSON-LD (LocalBusiness, FAQPage, ItemList, SportsActivityLocation, SportsEvent).
- Sport microsites got Twitter cards in Cycle 3.
- Sitemap and robots are tidy; ISR set on CMS-backed pages.
- Mobile sticky register CTA is in place.

## Gaps found this iteration

### P0 — fix this iteration
1. **Contact page heading hierarchy** — h1 → h3 (skipping h2) on the sidebar Location / Phone / Email blocks. SEO and screen readers prefer a strict ladder. Promoted to h2.
2. **Facility page missing BreadcrumbList JSON-LD** — Google won't render the breadcrumb rich snippet without it. Added.
3. **Game Day page missing BreadcrumbList JSON-LD** — same. Added.

### P1 — next iteration
- Hero CTA on `/page.tsx` could shrink padding on <375px so text doesn't wrap awkwardly.
- Sport pages could add BreadcrumbList too.
- Tournaments page ItemList could add AggregateOffer rollup.
- Inquire pages already have h1 — agent flagged a false positive; nothing to do.

### P2 — someday
- Compress + responsive-srcSet `courts-bg.jpg` (518KB → ~200KB target).
- Facility amenity grid 2-col→1-col on tiny phones.
- Extra `inert` on purely decorative background `<Image>`.

### BLOCKED — needs Mike
- `NEXT_PUBLIC_GOOGLE_REVIEW_URL` still placeholder; affects /leave-a-review CTA destination.

## Tasks chosen for ACT step
1. Promote contact sidebar h3 → h2 (3 sections).
2. Add BreadcrumbList JSON-LD to /facility.
3. Add BreadcrumbList JSON-LD to /gameday.

## Outcome
One commit on cycle/auto-improvements referencing this report.
